{
  "name": "hotel-price-tracker-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Hotel Price Tracker subscription management",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "stripe": "^15.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "wrangler": "latest"
  }
}
